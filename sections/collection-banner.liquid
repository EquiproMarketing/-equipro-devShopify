<div class="collection-banner page-width">
  <div class="collection-banner__inner">
    {%- assign banner_image = section.settings.custom_image | default: collection.image -%}
    
    {%- if banner_image != blank -%}
      <img src="{{ banner_image | image_url: width: 1600 }}" alt="{{ section.settings.custom_title | default: collection.title | escape }}" class="collection-banner__bg" loading="lazy">
    {%- else -%}
      <div class="collection-banner__placeholder"></div>
    {%- endif -%}

    <div class="collection-banner__overlay" style="opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};"></div>

    <div class="collection-banner__content">
      <h1 class="collection-banner__title">
        {%- if section.settings.custom_title != blank -%}
          {{ section.settings.custom_title | escape }}
        {%- else -%}
          {{ collection.title | default: "Wellness therapy" }}
        {%- endif -%}
      </h1>

      {%- assign description_text = section.settings.custom_description | default: collection.description -%}
      {%- if description_text != blank -%}
        <div class="collection-banner__desc">
          {{ description_text }}
        </div>
      {%- elsif section.settings.fallback_description != blank -%}
        <p class="collection-banner__desc">
          {{ section.settings.fallback_description | escape }}
        </p>
      {%- endif -%}
    </div>
  </div>
</div>

<style>
.collection-banner { margin-top: 1.5rem; margin-bottom: 2.5rem; }
.collection-banner__inner {
  position: relative;
  width: 100%;
  min-height: 260px;
  border-radius: 20px;
  overflow: hidden;
  display: flex;
  align-items: center;
}
.collection-banner__bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}
.collection-banner__placeholder {
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, #2c2c2c 0%, #1a1a1a 100%);
}
.collection-banner__overlay {
  position: absolute;
  inset: 0;
  background: #000000;
  transition: opacity 0.3s ease;
}
.collection-banner__content {
  position: relative;
  z-index: 2;
  padding: 3rem 3.5rem;
  max-width: 700px;
  color: #ffffff;
}
.collection-banner__title {
  font-size: 2.6rem;
  font-weight: 700;
  margin-bottom: 1rem;
  color: #a22938; /* Color vino/rojo característico de la referencia */
  letter-spacing: -0.02em;
}
.collection-banner__desc {
  font-size: 1.05rem;
  line-height: 1.55;
  color: #333333; /* Texto oscuro limpio sobre fondo claro en móvil o legible sobre overlay */
}
.collection-banner__desc p { margin: 0; }

@media (max-width: 767px) {
  /* En móvil quitamos la imagen de fondo y dejamos el diseño limpio tipo minimalista */
  .collection-banner__bg,
  .collection-banner__placeholder,
  .collection-banner__overlay {
    display: none !important;
  }

  .collection-banner__inner {
    background: transparent;
    border-radius: 0;
    flex-direction: column;
    min-height: auto;
    overflow: visible;
  }

  .collection-banner__content {
    background-color: #ffffff;
    padding: 1.5rem 0.5rem;
    max-width: 100%;
  }

  .collection-banner__title {
    font-size: 2.2rem;
    color: #a22938; /* Título en el tono rojo solicitado */
    margin-bottom: 0.75rem;
  }

  .collection-banner__desc {
    color: #555555; /* Descripción en tonos oscuros limpios */
    font-size: 1rem;
  }
}
</style>

{% schema %}
{
  "name": "Collection Banner",
  "tag": "section",
  "class": "section-collection-banner",
  "enabled_on": {
    "templates": ["collection"]
  },
  "settings": [
    {
      "type": "text",
      "id": "custom_title",
      "label": "Custom Title Override",
      "info": "Leave blank to dynamically use the Shopify Collection title."
    },
    {
      "type": "richtext",
      "id": "custom_description",
      "label": "Custom Description Override",
      "info": "Leave blank to dynamically use the Shopify Collection description."
    },
    {
      "type": "textarea",
      "id": "fallback_description",
      "label": "Fallback Description",
      "default": "Discover our range of high-quality professional heat therapy equipment designed for estheticians, spas, and wellness clinics, featuring precise temperature controls.",
      "info": "Used only if both Custom Description and Collection Description are empty."
    },
    {
      "type": "image_picker",
      "id": "custom_image",
      "label": "Custom Background Image",
      "info": "Overrides the featured collection image."
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 90,
      "step": 5,
      "unit": "%",
      "label": "Dark Overlay Opacity",
      "default": 40
    }
  ],
  "presets": [
    {
      "name": "Collection Banner"
    }
  ]
}
{% endschema %}